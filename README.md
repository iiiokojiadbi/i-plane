# i-plane

CLI для Plane — плотный вывод под агента, а не под глаза.

Обычный клиент печатает задачу целиком: двадцать девять полей, больше тысячи
токенов на штуку. Агенту из них нужны шесть. `i-plane` печатает строку.

## Состояние

Проектируется.
